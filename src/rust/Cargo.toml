[package]
name = "aerocapture"
version = "0.1.0"
edition = "2024"
description = "Trajectory simulation for aerocapture maneuvers"

[dependencies]
nalgebra = "0.34"
serde = { version = "1", features = ["derive"] }
toml = "0.8"

[dev-dependencies]

[profile.release]
opt-level = 3
lto = true
