# MSR outbound — Energy Controller GA training, 10 MC sims per evaluation
base = ["../missions/mars.toml", "common.toml"]

[guidance]
type = "energy_controller"
reference_bank_angle = 64.77026

[data]
results_suffix = ".train_energy_ctrl"
