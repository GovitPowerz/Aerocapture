# Study A -- CMA-ES on the dense_p515 control architecture (compute-matched n_pop=300).
# Runs natively here: 515 params < _CMAES_MAX_PARAMS (20000).
base = ["../sweep/dense_p515.toml"]

[data]
neural_network = "training_output/paper_opt_cmaes/best_model.json"
results_suffix = ".paper_opt_cmaes"

[optimizer]
algorithm = "cma_es"
