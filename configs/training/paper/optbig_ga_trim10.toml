# Curation-trim sub-study -- GA on dense_p3998, adaptive seeds, trim 10% of extremes.
base = ["../sweep/dense_p3998.toml"]

[data]
neural_network = "training_output/paper_optbig_ga_trim10/best_model.json"
results_suffix = ".paper_optbig_ga_trim10"

[optimizer]
algorithm = "ga"
curation_trim_fraction = 0.1
