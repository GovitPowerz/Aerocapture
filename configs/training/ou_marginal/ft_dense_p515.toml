# Fine-tune recipe: the frozen-trained champion's g20000 checkpoint continued
# 2000 gens under per_draw (the pilot showed this beats scratch retraining).
# The campaign runner seeds the checkpoint copy; target 22000 gens.
base = ["dense_p515.toml"]

[data]
neural_network = "training_output/ou_marginal/ft_dense_p515/best_model.json"
