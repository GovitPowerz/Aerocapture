# OU-marginal retrain: dense_p972 from scratch under per-draw noise seeding.
# Same cell regime (GA n_pop 60, adaptive seeds, training_n_sims 10); budget
# target 20000 gens = the deployed cell's budget (driven by the campaign runner).
base = ["../sweep/dense_p972.toml"]

[monte_carlo]
noise_seeding = "per_draw"

[data]
neural_network = "training_output/ou_marginal/dense_p972/best_model.json"

[checkpoints]
keep_last = 3
