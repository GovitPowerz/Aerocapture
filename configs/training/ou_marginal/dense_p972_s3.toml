# Seed repeat s3 (trainer RNG --seed 3; eval pools unchanged) of the
# per_draw scratch retrain -- sigma_run for the marginal architecture ranking.
base = ["dense_p972.toml"]

[data]
neural_network = "training_output/ou_marginal/dense_p972_s3/best_model.json"
