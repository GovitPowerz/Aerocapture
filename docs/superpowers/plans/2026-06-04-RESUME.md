# RESUME — Python + PyO3 Review Remediation

## How to find this work (git / worktree) -- READ FIRST
A fresh session usually starts in a DIFFERENT worktree (the harness auto-creates one per session at `main`), so it will NOT see these commits in its own working tree. The work lives here:

- **Worktree dir:** `/Users/govit/Git/Govit/Aerocapture/.claude/worktrees/wizardly-kalam-7cdd6c`
- **Branch:** `feature/python-pyo3-review-fixes` (HEAD `3d72e37`, base `main` @ `f678b9d`, 19 commits)
- **Main repo:** `/Users/govit/Git/Govit/Aerocapture` (on `main`). The `.git` object store is shared across all worktrees, so the branch ref and commits are reachable from anywhere in the repo.
- **NOT pushed** -- the branch is local-only (remote `origin` is GitHub, but there is no upstream). `git fetch` will NOT find it.

To continue, `cd` straight into the worktree where the branch is already checked out:
```bash
cd /Users/govit/Git/Govit/Aerocapture/.claude/worktrees/wizardly-kalam-7cdd6c
git branch --show-current                  # -> feature/python-pyo3-review-fixes
git log --oneline -1                       # -> 3d72e37 docs: update resume note ...
git log --oneline f678b9d..HEAD | wc -l    # -> 19
git status --short                         # -> clean
```
Locate it from anywhere in the repo with `git worktree list` (find the line tagged `[feature/python-pyo3-review-fixes]`).

Caveats:
- Do NOT run `git switch feature/python-pyo3-review-fixes` from another worktree -- git refuses, because the branch is already checked out in `wizardly-kalam-7cdd6c`. Just `cd` into that worktree.
- ONLY if the `wizardly-kalam-7cdd6c` worktree was removed: from the main repo run `git switch feature/python-pyo3-review-fixes` (the branch ref survives worktree deletion) and continue there.
- The committed plan/spec/resume docs are readable from any worktree without switching, e.g. `git show feature/python-pyo3-review-fixes:docs/superpowers/plans/2026-06-04-RESUME.md`.

**Branch:** `feature/python-pyo3-review-fixes` (off `main` @ `f678b9d`). Working tree clean. Non-slow suite green at **923 passed**. Zero Rust files touched so far (6 golden files definitionally intact). 19 commits landed.

## Documents
- Spec: `docs/superpowers/specs/2026-06-04-python-pyo3-review-fixes-design.md` (full finding inventory + IDs)
- Phase 0/1 plan: `docs/superpowers/plans/2026-06-04-python-pyo3-review-fixes.md`
- Phase 2 plan: `docs/superpowers/plans/2026-06-04-python-pyo3-review-fixes-phase2-dedup.md`
- Phases 3–6: roadmapped in the Phase 0/1 plan; author each as its own dated plan just-in-time.

## Done
- **Phase 1 — all 6 defects:** D1 `46b93bb`, D2 `922ac12`, D3 `40ed125`, D5 `1c0b650`, D4 `49141a0`, D6 `a5c84f3`. (Independently mergeable.)
- **Phase 2 — dedup, 7 of 11:**
  - H6 seed-registry `0da8fcb`
  - H11 apply_theme `cf34614`
  - H1 is_captured `34c0f5b`
  - H9 bin_indices `8c27250`
  - H5 typst_utils `b735540` + H5b completion (report_rl + nn_input_report) `e718bbc`
  - H10 NN-JSON unification `903359a` (also closed the train/eval normalization mismatch)
  - H2 route_param_path `33753eb`

## Next: resume at Phase 2 task **H4** (chart panel-builders + named (N,17) column constants)
Remaining Phase 2 order: **H4 → H3 → H7 → H8**, plus an **H2b cleanup** (below). Per-helper files/contract/acceptance are in the Phase 2 plan. Reviewer-subagent recommended for H4 (panel-builders, ~250-line charts.py refactor — visual-identity gate), H3 (cross-language `from_flat`, gated by the equivalence suite), H7 (Rust const exposure — **rebuild + golden bit-identity gate**), H8 (validation gate — train.py + island_model.py, the riskiest; reviewer mandatory).
- **H4**: `charts.py` named trajectory-column constants (`_TC_*`) + `_time_series_panel` / `_corridor_panel` builders; the 8 time-domain + 3 corridor functions become thin wrappers (keep names — the Typst template references them); `animate.py` reuses them. SVG-output-identical gate.
- **H3**: add `from_flat()` to the 6 `rl/layers/*.py` modules (inverse of each `to_flat`, mirroring Rust); collapse `warm_start._seed_policy_init` (95-line hand-rolled inverse) + `model_io.load_policy_from_json`. Gate: cross-language equivalence suite + a `to_flat∘from_flat==identity` test.
- **H7**: centralize the 52-element final-record index map in the Rust core (next to `finalize.rs`), reference from `aerocapture-py/results.rs` + `env.rs`; export `NN_FULL_INPUT_SIZE`/`DISPERSION_DRAW_LEN` from PyO3; replace inline `41`/etc. in sensitivity/ablation/parquet; add drift tests. Rebuild `maturin develop --release` + full `cargo test`.
- **H8**: extract `run_validation_gate(...)` shared by train.py single-algo + `island_model.validate_each`; lands the single-algo all-inf argmin guard (train.py ~1360 bare `np.argmin`). Add a test: single-algo all-inf population must not promote `pop[0]`.

Then Phases 3 (dead code), 4 (PyO3 `run_grid` + core Arc — Rust rebuild + golden bit-identity + dispersion-enabled cost-vector gate), 5 (decompositions), 6 (nits), 7 (smart-commit over the whole branch — the planned final step, NOT yet run).

## H2b — complete the routing dedup (small, do near H2)
H2 centralized `route_param_path` in `param_spaces.py` and migrated problem.py + compare_guidance.py (both blocks). THREE more files still have the same prefix→section if/elif chain:
- `report.py:~67` (best_params deploy routing)
- `train.py:~2317` (the `__main__` piecewise block — **diverges**: it adds a `command_shaping.enabled = True` side-effect; preserve it)
- `warm_start.py:~188` (scaffolding deploy routing)
Migrate each to `route_param_path` (preserving train.py's side-effect + each site's coercion). `evaluate.py` routing was deliberately LEFT (it builds nested dicts directly, not dot-paths — structurally different; migrating would be a rewrite, low value).

## Execution recipe (subagent-driven)
1. Build binding: `uv run maturin develop --release --manifest-path src/rust/aerocapture-py/Cargo.toml`
2. Per task: dispatch implementer (TDD where it adds behavior; bit-identity gate for pure dedup) → verify the diff against source → run the task's regression subset + `uv run pytest -q -m "not slow"` (must stay ≥923) → `ruff`/`mypy` → commit (stage only the task's files; trailer `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`; never `git add -A`).
3. Phase 4 only: re-capture a **dispersion-enabled** cost-vector baseline first (test_ref_orig.toml is dispersions-off, so its 8 seeds were identical — the seed→draw mapping isn't exercised by it). Then prove `run_grid` cost-vector bit-identical + record the speedup.

## Observations carried forward (fold in when the relevant file is touched)
- **warm_start.py:534** emits a real broadcasting warning: `mse_loss` target `[8,48,2]` vs input `[8,48,1]` — a latent loss-shape bug in `_chunked_bptt_train` (likely the `atan2_signed` 2-output vs 1-output target path). Investigate during H3 / Phase 5 G4. NOT yet addressed.
- **animate.py `costs_{k}` subpop npz reader** (lines ~62-72) is stale dead code (current trainer writes a flat `costs` array). Remove in Phase 3.
- Minor review notes intentionally left: D1 test lacks a call-count comment; D6 keeps a defensive `getattr`; H2 added `# type: ignore[return-value]` on `bin_indices` (mypy intp). All harmless.
- `/tmp/phase0_baseline/ftc_cost_vector.npy` is scratch (not committed; regenerate via the Phase 0 plan step if needed).
