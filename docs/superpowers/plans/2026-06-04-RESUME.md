# RESUME — Python + PyO3 Review Remediation

**Branch:** `feature/python-pyo3-review-fixes` (off `main` @ `f678b9d`). Working tree clean. Non-slow suite green at **916 passed**. Zero Rust files touched so far (6 golden files definitionally intact).

## Documents
- Spec: `docs/superpowers/specs/2026-06-04-python-pyo3-review-fixes-design.md` (full finding inventory + IDs)
- Phase 0/1 plan: `docs/superpowers/plans/2026-06-04-python-pyo3-review-fixes.md`
- Phase 2 plan: `docs/superpowers/plans/2026-06-04-python-pyo3-review-fixes-phase2-dedup.md`
- Phases 3–6: roadmapped in the Phase 0/1 plan; author each as its own dated plan just-in-time.

## Done
- **Phase 1 — all 6 defects:** D1 `46b93bb`, D2 `922ac12`, D3 `40ed125`, D5 `1c0b650`, D4 `49141a0`, D6 `a5c84f3`. (Independently mergeable.)
- **Phase 2 — dedup, 3 of 11:** H6 seed-registry `0da8fcb`, H11 apply_theme `cf34614`, H1 is_captured `34c0f5b`.

## Next: resume at Phase 2 task **H9** (`binned_reduce`)
Remaining Phase 2 order (ascending risk): **H9 → H5 → H10 → H2 → H4 → H3 → H7 → H8**. Per-helper files/contract/acceptance are in the Phase 2 plan. Reviewer-subagent recommended for H2 (routing semantics), H4 (panel-builders), H3 (cross-language `from_flat`), H7 (Rust const exposure), H8 (validation gate). Then Phases 3 (dead code), 4 (PyO3 `run_grid` + core Arc — Rust rebuild + golden bit-identity gate), 5 (decompositions), 6 (nits), 7 (smart-commit over the whole branch — the planned final step, NOT yet run).

## Execution recipe (subagent-driven)
1. Build binding: `uv run maturin develop --release --manifest-path src/rust/aerocapture-py/Cargo.toml`
2. Per task: dispatch implementer (TDD where it adds behavior; bit-identity gate for pure dedup) → verify the diff against source → run the task's regression subset + `uv run pytest -q -m "not slow"` (must stay ≥916) → `ruff`/`mypy` → commit (stage only the task's files; trailer `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`; never `git add -A`).
3. Phase 4 only: re-capture a **dispersion-enabled** cost-vector baseline first (test_ref_orig.toml is dispersions-off, so its 8 seeds were identical — the seed→draw mapping isn't exercised by it). Then prove `run_grid` cost-vector bit-identical + record the speedup.

## Observations carried forward (fold in when the relevant file is touched)
- **warm_start.py:534** emits a real broadcasting warning: `mse_loss` target `[8,48,2]` vs input `[8,48,1]` — a latent loss-shape bug in `_chunked_bptt_train` (likely the `atan2_signed` 2-output vs 1-output target path). Investigate during Phase 2 H10 / Phase 5 G4 (warm_start work). NOT yet addressed.
- **animate.py `costs_{k}` subpop npz reader** (lines ~62-72) is stale dead code (current trainer writes a flat `costs` array; islands npz uses different keys). Remove in Phase 3.
- Minor review notes intentionally left: D1 test lacks a call-count comment + broader seed-strategy coverage; D6 keeps a defensive `getattr(self, "_consecutive_eval_failures", 0)` (the implementer's deliberate defense-in-depth). Both harmless.
- `/tmp/phase0_baseline/ftc_cost_vector.npy` is scratch (not committed; regenerate via the Phase 0 plan step if needed).
