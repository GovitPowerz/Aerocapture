# RESUME — Python + PyO3 Review Remediation

## How to find this work (git / worktree) -- READ FIRST
A fresh session usually starts in a DIFFERENT worktree (the harness auto-creates one per session at `main`), so it will NOT see these commits in its own working tree. The work lives here:

- **Worktree dir:** `/Users/govit/Git/Govit/Aerocapture/.claude/worktrees/wizardly-kalam-7cdd6c`
- **Branch:** `feature/python-pyo3-review-fixes` (base `main` @ `f678b9d`; HEAD = the latest commit on this branch, which advances as work continues -- do not rely on a pinned SHA here)
- **Main repo:** `/Users/govit/Git/Govit/Aerocapture` (on `main`). The `.git` object store is shared across all worktrees, so the branch ref and commits are reachable from anywhere in the repo.
- **NOT pushed** -- the branch is local-only (remote `origin` is GitHub, but there is no upstream). `git fetch` will NOT find it.

To continue, `cd` straight into the worktree where the branch is already checked out:
```bash
cd /Users/govit/Git/Govit/Aerocapture/.claude/worktrees/wizardly-kalam-7cdd6c
git branch --show-current                  # -> feature/python-pyo3-review-fixes
git log --oneline -1                       # -> latest remediation commit (NOT f678b9d, which is main's tip)
git log --oneline f678b9d..HEAD | wc -l    # -> count of remediation commits (grows as work continues)
git status --short                         # -> clean
```
Locate it from anywhere in the repo with `git worktree list` (find the line tagged `[feature/python-pyo3-review-fixes]`).

Caveats:
- Do NOT run `git switch feature/python-pyo3-review-fixes` from another worktree -- git refuses, because the branch is already checked out in `wizardly-kalam-7cdd6c`. Just `cd` into that worktree.
- ONLY if the `wizardly-kalam-7cdd6c` worktree was removed: from the main repo run `git switch feature/python-pyo3-review-fixes` (the branch ref survives worktree deletion) and continue there.
- The committed plan/spec/resume docs are readable from any worktree without switching, e.g. `git show feature/python-pyo3-review-fixes:docs/superpowers/plans/2026-06-04-RESUME.md`.

**Branch:** `feature/python-pyo3-review-fixes` (off `main` @ `f678b9d`). Working tree clean. **ALL PHASES 1-7 COMPLETE — branch ready for review/merge (NOT pushed; local-only).** Non-slow suite green at **976 passed** (0 failed); full Rust suite green (527 unit + integration, 6 guidance goldens bit-identical); `lint_code.sh` FULLY GREEN ("Success: no issues found in 208 source files"). **Phase 7 DONE (`32d0913`): smart-commit synced CLAUDE.md to the whole branch (run_grid + final_record_indices API, atan2 2-output validation, removed stale R3/R4 refs, charts count 24->25); README needed no changes (run_batch example still valid, run_grid is internal). 69 commits since main.** Nothing left — the remediation is complete.

### Phase 5 progress (this session)
- **G4 (`0888435`)** — decomposed `warm_start.build_warm_start_chromosome` into `_collect_supervisor_corpus` / `_write_selection_sidecar` / `_encode_and_persist`. Verified byte-identical moved regions + reviewer-approved. Gate: 6 slow per-arch warm_start tests + fast warm_start suites.
- **G3-3a (`7619a39`)** — deduped scaffolding-override + eval-TOML: added `param_spaces.route_scaffolding_param` (the `lateral.max_reversals` int-coerce + `route_param_path`, was open-coded 3x), `compare_guidance._apply_optimized_params_to_toml` (collapsed 2 near-identical best_params→toml blocks, unit-tested in new `tests/test_compare_guidance_apply_params.py`), `report._resolve_eval_toml` (shared by run_final_evaluation + _run_undispersed_nominal).
- **G3-3b (`37ccbd7`)** — split `report.generate_report` into COMPUTE (`_compute_report_payload` + `ReportPayload`) / RENDER (`_render_report_assets`) / COMPILE. `_generate_trajectory_charts` is now a pure renderer (nominal+corridor hoisted into compute as params); `render_mission_performance_charts` computes them inline so its signature + `report_rl.py` are unchanged. Only observable delta: training-chart stdout interleaves after the eval compute (accepted). USER DECISION: the full ReportPayload seam was explicitly chosen over the "dedups-only" descope despite being single-caller.
- **G2 (`40559db` + `2023778`)** — extracted `build_critic_from_architecture` + `collect_rollout` from `_run_ppo` (PPO-only, both pure moves verified). DELIBERATELY SKIPPED the PPO/SAC shared-tail unification: `_run_sac` has NO test coverage (no test calls it) + a unified validate/checkpoint/record tail needs 4+ callbacks (over-engineering) + spec §8 defers SAC work. `_run_sac` left byte-identical (verified 3 ways). The plan's own contingency sanctions this. Gate: PPO smoke/regression/chunk-invariant + rl/test_train_smoke (main()).
- **G1 (`7b1e9f7` + `e70f69f` + `01e3516`)** — decomposed `train()` into `_setup_param_specs` (param-spec construction), `_emit_warm_start_artifacts` (3 best-effort warm-start sidecar try/excepts), `_build_initial_population` (the resume/v1/v2/non-NN population block, recomputes warm_start_active/bound_mult internally, MUTATES param_specs in place via element-write). 🚫 The `best_overall_*` unpacking + the `if best_overall_individual is None:` guard stayed byte-unchanged in `train()` (cross-gen cost-incomparability invariant). REVIEWER-APPROVED (independent subagent confirmed invariant intact + in-place mutation visible through problem + no stranded names). Net diff +317/-267, pure cut-and-paste. (Commit 1 needed a transient warm_start_active/bound_mult recompute to stay valid in isolation; Commit 3 removed it — net-zero, confirmed no stranding at HEAD.) Gate: test_train_interrupt (incl. TestResumePreservesCheckpointedBest) + test_train_no_validation_promotion + test_validation_gate + test_warm_start_optimizer_seeding + test_resume_enhancements, all green.

### Phase 5 verification (final)
Full non-slow suite **968 passed, 0 failed**; 6 Rust guidance goldens bit-identical; tree clean. Per-task discipline followed: each commit staged only its files (never `git add -A`); every subagent diff verified against source by the controller; G1 + G4 additionally reviewer-subagent-approved; never pushed.

### Phase 6 COMPLETE — low nits (10 commits)
All user-selected scope done. Batch 1 (`8d748de` N1 except-parens, `38cd9d5` N2 asinh activation + test, `2c7051d` N4 log previously-silent excepts, `0159d04` N5 io `.stat()` `.exists()` guards + tests, `0a0bb31` N7 public `save_line_chart` + dropped report_rl type-ignores). Batch 2 (`31a6976` N6 vectorize `population_diversity` via pdist + equivalence test, `7b9e2e3` N3 seeded PPO minibatch shuffles + checkpoint-persisted rng + repro test). Batch 3 (`6bbddc7` corrected `run_batch` thread-pool doc, `dc4115a` atan2_signed-requires-2-output-head load-time validation in BOTH Rust `validate_output_parameterization` + Python `NetworkConfig.__post_init__`, golden-gated — repurposed the Phase-5 mse_loss test to assert at NetworkConfig construction; the warm_start.py BPTT guard remains defense-in-depth for post-construction mutation). Batch 4 (`3c8e01f` cleared pre-existing ruff-format + mypy debt across the test suite -> lint_code.sh fully green). N8 (parquet citation) + N9 (problem.py temp-JSON) were ALREADY resolved by Phase 2 H7 / Phase 4 run_grid -- skipped. All diffs controller-verified; 6 goldens re-confirmed bit-identical after the Rust-touching atan2 change.

### Phase 6 (was-NEXT) — historical triage notes
Triage from spec + carried-forward observations: N1 `warm_start_report.py:240` `except TypeError, ValueError:` -> add parens; N2 `policy._ACT` missing `asinh` (KeyError on asinh activation); N3 RL has no RNG seeding (`ppo_update_bptt` shuffles via global np.random) -- a real reproducibility feature, NOT a 1-liner (checkpoint-format change), assess scope; N4 narrow silent excepts (log at min) at train.py rng-restore / _read_constraint_limits / evaluate.py:159,198 / nn_input_report mask; N5 io/ parsers `.stat()` -> guard with `.exists()`; N6 vectorize `metrics.population_diversity` O(n^2); N7 promote `charts._save_line_chart`, drop 6 `# type: ignore` in report_rl.py; N8 `parquet_output.py:17` stale `runner.rs:833` citation -> function name; N9 problem.py temp-NN-JSON helper (largely subsumed by P6/H2 -- verify if anything remains). PLUS carried: stale `batch::run_batch` doc-comment ("scoped Rayon pool" -> only when n_threads.is_some()); the atan2_signed output_size==2 defense-in-depth validation (NetworkConfig.__post_init__ + Rust validate_output_parameterization, golden-gated); PRE-EXISTING lint debt (test_charts.py ruff-format, test_route_param.py + rl/test_describe_architecture.py mypy) -- pre-dates the branch, fix as a hygiene commit if cheap. Then Phase 7: smart-commit over the whole branch.

## Documents
- Spec: `docs/superpowers/specs/2026-06-04-python-pyo3-review-fixes-design.md` (full finding inventory + IDs)
- Phase 0/1 plan: `docs/superpowers/plans/2026-06-04-python-pyo3-review-fixes.md`
- Phase 2 plan: `docs/superpowers/plans/2026-06-04-python-pyo3-review-fixes-phase2-dedup.md`
- Phases 3–6: roadmapped in the Phase 0/1 plan; author each as its own dated plan just-in-time.

## Done
- **Phase 1 — all 6 defects:** D1 `46b93bb`, D2 `922ac12`, D3 `40ed125`, D5 `1c0b650`, D4 `49141a0`, D6 `a5c84f3`. (Independently mergeable.)
- **Phase 2 — dedup, 11 of 11 (COMPLETE):**
  - H6 seed-registry `0da8fcb`
  - H11 apply_theme `cf34614`
  - H1 is_captured `34c0f5b`
  - H9 bin_indices `8c27250`
  - H5 typst_utils `b735540` + H5b completion (report_rl + nn_input_report) `e718bbc`
  - H10 NN-JSON unification `903359a` (also closed the train/eval normalization mismatch)
  - H2 route_param_path `33753eb`
  - H4 chart panel-builders + named (N,17) `_TC_*` columns `3a2de3b` (SVG byte-identical, verified by render diff)
  - H2b route_param_path migration (report/train/warm_start deploy routing) `ffa12f1`
  - H3 per-layer `from_flat()` on all 6 layer modules + collapsed the hand-rolled inverses `eb24414`
  - H7 final-record index map (Rust `simulation/final_record.rs` + `FINAL_RECORD_LEN`) + PyO3 `NN_FULL_INPUT_SIZE`/`DISPERSION_DRAW_LEN` + drift tests `7783957` (golden bit-identical; first Rust-touching task)
  - H8 shared `evaluate.run_validation_gate` + single-algo all-inf/NaN selection guard `f686b4f`
- **Phase 3 — dead-code removal, 5 of 5 (COMPLETE):** R1 delete `plotting/` pkg `8f9db80`, R2 remove `evaluate_population_per_seed` `bc3b70a`, R3 delete orphaned `chart_sobol_convergence` `41c903e`, R4 delete `derive_asinh_scale` `dce20f3`, R5 remove `CAPTURE_COST_THRESHOLD` + fix `log_cap` docstring `b57665c`. Net -792 lines; suite 957 (2 dead tests removed). animate.py `costs_{k}` reader KEPT (backward-compat, not dead — see roadmap note). H7 follow-up `a117fbb` (final_record_indices() + drift) also landed.
- **Phase 4 — PyO3 `run_grid` + core `Arc`-tables, 4 of 4 (COMPLETE):** Task 1 (core `Arc` tables + `from_toml_with_tables` + `run_for_api_cell`) `9cbb927`, Task 2 (run* hardening: P1 GIL detach, P3 single-copy getters, P4 strided draws, P5 global pool) `981a632`, Task 3 (`run_grid` binding + in-memory NN, P2/P6 — also enabled the `serde_json` `float_roundtrip` feature in `src/rust/Cargo.toml` so in-memory NN weights serialize bit-identically to the temp-JSON path; measured impact is ~1e-6 boundary-only, nominal goldens stay within 1e-9) `86a8b93`, Task 4 (rewire `problem._run_batch_pyo3` to one `run_grid` call + extract `evaluate.build_v2_architecture`) `092a644`, plus a follow-up `2683231` ruff-format + mypy-annotate of the Phase-4-introduced test files (`test_pyo3.py` strided test, `test_run_grid.py`). The real consolidated-NN cost vector (dispersions + density_perturbation ON, 8×6 grid) is **bit-identical** (`np.array_equal`) to the post-`float_roundtrip` per-seed `run_batch` baseline; measured ~3x speedup on the random-weight smoke grid (overhead-dominated — random weights crash fast, so this floors the real-population gain from the once-per-individual SimData build). Non-NN bit-identity locked by `test_problem.py::test_run_batch_pyo3_matches_per_seed_run_batch_non_nn`.
  - **Phase-4 follow-up nits (all 3 done):** cold paths (`evaluate_individual_per_seed` / `_records_per_seed`, used by the validation gate + gen-0 baseline) unified onto `run_grid` via a shared `_run_grid_records` helper — **temp-JSON + per-seed loop fully removed from problem.py** (`tempfile`/`os` imports gone), bit-identity locked by a new non-NN test `21ff71e`; `run_grid` now raises `ValueError` if `monte_carlo.seed`/`simulation.n_sims` leak into the overrides (it owns the seed axis) `503ef1d`; deleted the untracked `.phase4_baseline_capture.py` scratch script. So problem.py now has ONE evaluation path (all of hot + validation + baseline go through `run_grid`).

## Next: Phase 5 (Phases 1-4 COMPLETE)
Phase 2 (all of H1–H11) is done and verified end-to-end: full non-slow suite **956 passed**, full `cargo test` green (incl. the 3 `test_rust_golden` + 6 `guidance_regression` goldens, bit-identical), `ruff`/`mypy` clean, no import cycles, no leftover dead code, tree clean. Each task was implementer→spec-review→quality-review→fix; H7 + H8 had post-review cleanups folded in. 25 commits since `main`.

Remaining roadmap (author each as its own dated plan just-in-time; the design spec has the full finding inventory):
- **Phase 3** — DONE (see Done above). DECISION: the animate.py `costs_{k}`/`n_subpops` subpop npz reader was deliberately KEPT, not deleted — it is backward-compat for legacy on-disk checkpoints (paired with `_discover_checkpoints`'s legacy `checkpoint_r*` glob; the test fixtures still exercise it). Not provably dead, so removing it would risk breaking animation of old training runs. Plan: `docs/superpowers/plans/2026-06-04-python-pyo3-review-fixes-phase3-deadcode.md`.
- **Phase 4** — DONE (see Done above). PyO3 `run_grid` + core `Arc` tables; the GA hot path (`problem._run_batch_pyo3`) now issues one GIL-releasing `run_grid` call over the full individual×seed grid (NN weights in-memory, no temp JSON per individual). Real consolidated-NN cost vector bit-identical to the post-`float_roundtrip` per-seed baseline. Plan: `docs/superpowers/plans/2026-06-04-python-pyo3-review-fixes-phase4-rungrid.md`.
- **Phase 5** — decompositions. Includes the warm_start.py:534 `mse_loss` broadcasting bug (see Observations).
- **Phase 6** — nits.
- **Phase 7** — `smart-commit` over the whole branch (the planned FINAL step, NOT yet run).

## H2b — DONE (`ffa12f1`)
Migrated report.py / train.py (`__main__` piecewise) / warm_start.py deploy routing to `route_param_path`. CORRECTION to the prior note: the side-effect is NOT train.py-only — ALL THREE sites carry `command_shaping.enabled = True`, and all three preserve it caller-side (`route_param_path` returns only the dot-path, so the `shaping.`→`enabled=True` side-effect can't move into the helper). report.py also has a subtlety: it has NO `else` branch (unprefixed keys are SKIPPED), preserved via a `SCAFFOLDING_PREFIXES` gate. `evaluate.py` routing deliberately LEFT (nested dicts, not dot-paths). Locked by `tests/test_route_param_deploy.py`.

## Execution recipe (subagent-driven)
1. Build binding: `uv run maturin develop --release --manifest-path src/rust/aerocapture-py/Cargo.toml`
2. Per task: dispatch implementer (TDD where it adds behavior; bit-identity gate for pure dedup) → verify the diff against source → run the task's regression subset + `uv run pytest -q -m "not slow"` (must stay ≥956) → `ruff`/`mypy` → commit (stage only the task's files; trailer `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`; never `git add -A`).
3. Phase 4 only: re-capture a **dispersion-enabled** cost-vector baseline first (test_ref_orig.toml is dispersions-off, so its 8 seeds were identical — the seed→draw mapping isn't exercised by it). Then prove `run_grid` cost-vector bit-identical + record the speedup.

## Observations carried forward (fold in when the relevant file is touched)
- **warm_start.py:534** emits a real broadcasting warning: `mse_loss` target `[8,48,2]` vs input `[8,48,1]` — a latent loss-shape bug in `_chunked_bptt_train` (likely the `atan2_signed` 2-output vs 1-output target path). Investigate in **Phase 5** (H3 correctly did NOT touch it — it's a training-loop loss-shape issue, unrelated to per-layer `from_flat`). Still emits at suite runtime (16 warnings). NOT yet addressed.
- **animate.py `costs_{k}` subpop npz reader** (lines ~62-72) is stale dead code (current trainer writes a flat `costs` array). Remove in Phase 3.
- Minor review notes intentionally left: D1 test lacks a call-count comment; D6 keeps a defensive `getattr`; H2 added `# type: ignore[return-value]` on `bin_indices` (mypy intp). All harmless.
- `/tmp/phase0_baseline/ftc_cost_vector.npy` is scratch (not committed; regenerate via the Phase 0 plan step if needed).

### Discovered during Phase 4 (fold into Phase 5/6)
- **`serde_json` `float_roundtrip` (Phase 4, `86a8b93`) — a core-crate numerics decision worth a human glance.** Enabled in `src/rust/Cargo.toml` because serde_json's DEFAULT f64 *deserializer* is not bit-exact (it does `significand as f64 * POW10[exp]`, up to ~1 ULP off), so `save_json`→load (every deployed `best_model.json` / `compare_guidance` / `report` load) silently parsed NN weights ~1 ULP off the trained chromosome. The feature switches to lexical's correctly-rounded parser → exact round-trip, which was REQUIRED for the in-memory `run_grid` NN path to be bit-identical to the temp-JSON path (P6) and is also a latent-bug fix. Deserializer-only (no serialization/JSON-golden churn); scope limited to the 4 NN-model `from_str` sites in `data/neural`. Measured impact: dispersed NN cost vector shifts by max rel ~9.6e-7 on ONE boundary-sensitive (crash-regime) individual; the deterministic neural golden stays within its `rel_tol=1e-9` (all 6 guidance goldens pass). Reversible (1-line) if the user wants strict pre-Phase-4 byte-identity over P6 exactness — but the post-fix behavior is strictly more correct. Verified from serde_json 1.0.150 source.
- **Pre-existing lint debt (NOT Phase-4-introduced; surfaced by `lint_code.sh` in Task 4) — candidate for Phase 6 / a hygiene commit.** Present verbatim at `d101bee`, untouched by Phase 4: `tests/test_charts.py` fails `ruff format --check`; `tests/test_route_param.py` (7×) + `tests/rl/test_describe_architecture.py` (3×, incl. `BaseModel has no attribute hidden_size/input_size`) fail `mypy src tests`. `lint_code.sh`/CI runs `mypy ... src tests`, so these would fail CI unless CI uses a narrower scope. The Phase-4-introduced lint items (test_pyo3.py format, test_run_grid.py format + 2 annotations) were fixed in `2683231`.
- ~~`run_grid` does not enforce its documented no-`monte_carlo.seed`/no-`simulation.n_sims` override precondition (Task-3 review Minor)~~ **RESOLVED by `503ef1d`** (run_grid now raises `ValueError` if those keys leak).
- **`batch::run_batch` doc-comment is stale** (Task-2 review Minor): says "Uses a scoped Rayon thread pool with `n_threads` threads" but now only builds a scoped pool when `n_threads.is_some()` (else global pool). One-line doc fix (Phase 6).
- **`problem._build_grid_overrides` is a near-duplicate of `_build_overrides`'s NN-skip branch** (differs only by the trailing seed/n_sims injection). Deliberately left separate (the two override builders are intentionally distinct); flagged, not a defect.
